{
{% for vkey in verification_keys.results %}
    "{{ vkey.chain }}": {
        "multisig": "{{ vkey.stdout_lines[0] }}",
        "multisigfee": "{{ vkey.stdout_lines[1] }}"
    }{% if not loop.last %},{% endif %}
{% endfor %}
}